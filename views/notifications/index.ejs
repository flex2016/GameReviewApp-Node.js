<% include ../partials/header %> 

<h1>Notifications</h1>

<ul>
	<% allNotifications.forEach(function(notification) { %>
	<li>
		<a href="/games/<%= notification.gameId %>">
			<%= notification.username %> created a new game %>
		</a>
	</li>
	<% }); %>
</ul>

<% include ../partials/footer %> 